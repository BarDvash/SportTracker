<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.technion.fitracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity"></activity>
        <activity android:name=".user.personal.workout.WorkoutStarter" />
        <activity android:name=".user.personal.workout.edit.EditAerobicExercise" />
        <activity android:name=".user.personal.workout.edit.EditWeightExercise" />
        <activity android:name=".UserLandingPageActivity" />
        <activity android:name=".user.personal.workout.edit.AddExerciseActivity" />
        <activity android:name=".user.personal.workout.edit.CreateNewWorkoutActivity" /> <!-- enable the search dialog to send searches to SearchableActivity from every other activity -->
        <meta-data
            android:name="android.app.default_searchable"
            android:value=".SearchableActivity" />

        <provider
            android:name=".MySuggestionProvider"
            android:authorities="android.technion.fitracker.MySuggestionProvider" />

        <activity android:name=".user.business.BusinessUserActivity" />
        <activity android:name=".user.personal.UserActivity" />
        <activity
            android:name=".login.FlashSignInActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.personal.nutrition.NutritionAddMealActivity" />
        <activity android:name=".user.personal.nutrition.NutritionAddDishFragment" /> <!-- this is the searchable activity; it performs searches -->
        <activity
            android:name=".SearchableActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".user.personal.measurements.MeasurementsHistoryActivity" />
    </application>

</manifest>